kind: VirtualService
apiVersion: networking.istio.io/v1alpha3
metadata:
  name: chris
spec:
  hosts:
    - 'christest.dev.demandbase.com'
  gateways:
    - chris-gateway
  http:
    - match:
        - uri:
            exact: /
      route:
        - destination:
            host: chris-nginx-service.default.svc.cluster.local
            port:
              number: 80
            subset: v1
          weight: 100
