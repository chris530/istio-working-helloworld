### 
* Destination rule breaks the service into pieces 

### Content based routing,   istio can inspect traffic and make requests
* 
   http:
   - match:
     - headers:
         foo:
           exact: bar
           
           
### Circuit break,  "trip" the breaker if failures happen, ex limit max connections

### Fault Injection ( Chaos engineering ) , purpusly break connections between microservices


### Gateway

Allows us to define ingress gateway , ( traffic will come in through here ) 


### Egress ServiceEntry 

1.1 Disables the egress gateway ?

Need to allow outgoing traffic

You can also for example force TLS

